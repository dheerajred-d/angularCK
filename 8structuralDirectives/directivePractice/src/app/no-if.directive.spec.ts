import { NoIfDirective } from './no-if.directive';

describe('NoIfDirective', () => {
  it('should create an instance', () => {
    const directive = new NoIfDirective();
    expect(directive).toBeTruthy();
  });
});
