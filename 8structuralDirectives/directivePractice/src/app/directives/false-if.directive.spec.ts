import { FalseIfDirective } from './false-if.directive';

describe('FalseIfDirective', () => {
  it('should create an instance', () => {
    const directive = new FalseIfDirective();
    expect(directive).toBeTruthy();
  });
});
